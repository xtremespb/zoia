{
    "type": "object",
    "properties": {
        "page": {
            "type": "number",
            "minimum": 1,
            "maximum": 999999
        },
        "sortId": {
            "type": "string",
            "enum": ["username", "email"]
        },
        "sortDirection": {
            "type": "string",
            "enum": ["asc", "desc"]
        }
    },
    "required": ["page", "sortId", "sortDirection"]
}
